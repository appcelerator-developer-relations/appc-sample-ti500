'TextArea': {

	// We just want to display text in a way that it can scroll
	// We don't want the user to actually be able to edit text
	editable: false,

	width: Ti.UI.FILL,
	height: Ti.UI.FILL,
	font: {
		fontFamily: 'Courier New'
	}
}
